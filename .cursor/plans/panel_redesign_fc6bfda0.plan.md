# Right-Panel Redesign: Chat · Agents · Runs

See project transcript for full plan. Implementation tracks:

- **Chat** — conversational AI; compact `RunStatusChip`; no inline pipeline controls
- **Agents** — pipeline workspace; `RunActionBar` is the sole control surface
- **Runs** — history & audit; master-detail with filters and search

Default `agentPanelPlacement` is `right` so CHAT / AGENTS / RUNS tabs are visible together.
