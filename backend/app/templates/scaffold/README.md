# {{PROJECT_NAME}}

{{PROJECT_DESCRIPTION}}

## Setup

1. Copy `.env.example` to `.env`
2. Install dependencies per stack (`{{STACK}}`)
3. Run `{{DEV_SERVER_COMMAND}}`

## Tests

```bash
{{TEST_COMMAND}}
```

Copyright {{YEAR}} {{AUTHOR}}.
